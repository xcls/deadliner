<%= render 'deadlines/header', project: project, deadline: deadline %>

<h4>Tasks</h4>
<% tasks.each do |task| %>
  <h5><%= task.title %></h5>
  <p>State: <%= task.state %></p>
  <%= sanitize task.description_html %>
<% end %>
